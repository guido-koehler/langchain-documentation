# Core LangChain / LangGraph stack
langgraph==0.2.56
langchain==0.3.14
langchain-anthropic>=0.3.0

# OpenTelemetry tracing support (used by langchain-azure-ai for Foundry observability)
langchain-azure-ai[opentelemetry]>=0.1.0

# LangSmith tracing (optional but strongly recommended)
langsmith==0.1.147

# Azure authentication (used for Key Vault and other Azure resources)
azure-identity>=1.17.0

# Utilities
python-dotenv==1.0.1
pydantic==2.10.4
